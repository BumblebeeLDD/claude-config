# Global instructions (all projects)

These apply to **every** Claude Code project on this machine. Project-level `CLAUDE.md` /
`memory/` may add to or override anything below for that specific project.

## Guidelines (shared, single source of truth)
Every project starts from and operates by the guidelines. They live in one canonical,
editable file and are imported here so they load globally — edit that file and all projects follow
the update automatically:

@~/.claude/guidelines/guidelines.md
